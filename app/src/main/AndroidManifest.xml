<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.sistempakarhydroponics">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".InputDatabase"></activity>
        <activity android:name=".DummyInput" />
        <activity android:name=".CekTanaman" />
        <activity android:name=".PilihTanaman" />
        <activity android:name=".SettingAntares" />
        <activity android:name=".Home" />
        <activity android:name=".SignUpDone" />
        <activity android:name=".SignUp" />
        <activity android:name=".GetStarted" />
        <activity android:name=".SplashAct">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>